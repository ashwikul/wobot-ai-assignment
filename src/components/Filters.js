import React from "react";

function Filters() {
  return (
    <div>
      <select>
        <option>location</option>
      </select>
      <select>
        <option>status</option>
      </select>
    </div>
  );
}

export default Filters;
